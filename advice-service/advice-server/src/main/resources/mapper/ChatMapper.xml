<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zyq.chirp.adviceserver.mapper.ChatMapper">
    <insert id="insertBatch">
        insert into tb_private_message(id, sender_id, receiver_id,conversation_id, content, type, create_time, status)
        VALUES
        <foreach collection="messages" item="message" separator=",">
            (#{message.id},
            #{message.senderId},
            #{message.receiverId},
            #{message.conversationId},
            #{message.content},
            #{message.type},
            #{message.createTime} ,
            #{message.status})
        </foreach>
    </insert>
</mapper>